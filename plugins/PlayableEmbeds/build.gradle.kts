version = "0.0.0"
description = "Makes Spotify and Youtube Embeds playable - HOMEMADE FORK"

aliucord { 
    changelog.set("""
        # 1.0.2
        * Youtube embed now supports timestamps
    """.trimIndent())
}
